# Fix OCR Extraction Failure\n\n## Background\nThe OCR extraction is consistently failing and hitting the Assisted Review fallback payload. Through deep tracing, I have identified that the root cause is a strict **18-second timeout** in the frontend ForecastService.ocrScan mechanism.\n\nWhen the Supabase Edge Function (i-inference) attempts to process the image, it tries a primary model (like gemini-3.8-flash). If that model is slow or experiences a hiccup, the Edge Function intelligently falls back to the next model in its chain. However, because the frontend strictly aborts the connection after exactly 18 seconds (cController.abort()), it kills the connection before the Edge Function can finish its fallback processing. This forces the UI to gracefully degrade to the Scan Pending fallback state.\n\n## Proposed Changes\n\n### [MODIFY] rontend/src/services/forecastService.ts\n1. **Increase Frontend OCR Timeout:** I will increase the cTimeout in Tier 2 from 18000 (18s) to 45000 (45s). This will give the Edge Function sufficient time to execute its internal fallback chain and return the structured JSON without the frontend prematurely terminating the request.\n2. **Robust JSON Extraction:** I will enhance the JSON regex extractor to handle edge cases where the AI might return slightly malformed JSON, ensuring that we recover as much data as possible even if the AI hallucinates outside the JSON block.\n\n## User Review Required\nThis change directly touches orecastService.ts. As per Rule 1.1 and 1.3, I am requesting your explicit permission to proceed with these structural stability fixes to resolve the OCR failure.\n
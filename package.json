{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test:invariants": "node scripts/verify-webhook-invariants.cjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "puppeteer": "^25.1.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2"
  }
}
